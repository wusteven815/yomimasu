import { ResponsiveNav } from "@/components";

export default function Page() {
  return (
    <ResponsiveNav currentTitle="Updates">
      <ResponsiveNav.Body>
        <p>Body</p>
      </ResponsiveNav.Body>
    </ResponsiveNav>
  );
}
